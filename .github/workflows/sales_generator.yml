# .github/workflows/manual_sales_generation.yml
# Sales Monitoring Engine - Manual Workflow (CORREGIDO)
# Trigger: manual dispatch only (workflow_dispatch)

name: Sales Monitoring Engine - Manual Generation

on:
  workflow_dispatch:
    inputs:
      target_date:
        description: 'Target date (YYYY-MM-DD) - Leave empty for yesterday'
        required: false
        type: string
        default: ''
      days_to_generate:
        description: 'Number of days to generate (1-30)'
        required: false
        type: number
        default: 1
      database_name:
        description: 'Database file name'
        required: false
        type: string
        default: 'sales_monitoring.db'

permissions:
  contents: write

jobs:
  generate-sales-data:
    name: Generate Sales Data
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Verify script location
        run: |
          if [ -f "scripts/sales_generator.py" ]; then
            echo "✅ Script found: scripts/sales_generator.py"
            ls -la scripts/
          else
            echo "❌ ERROR: scripts/sales_generator.py not found"
            ls -la
            exit 1
          fi
      
      - name: Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas numpy openai
      
      - name: Verify DeepSeek API configuration
        env:
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
        run: |
          if [ -z "$DEEPSEEK_API_KEY" ]; then
            echo "⚠️ WARNING: DEEPSEEK_API_KEY not found - running in OFFLINE mode"
          else
            echo "✅ DeepSeek API key found - Intelligent mode ACTIVE"
          fi
      
      - name: Generate sales data
        id: generate
        env:
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
        run: |
          TARGET_DATE="${{ github.event.inputs.target_date }}"
          DAYS="${{ github.event.inputs.days_to_generate }}"
          DB_NAME="${{ github.event.inputs.database_name }}"
          
          echo "============================================"
          echo "Sales Monitoring Engine - Data Generation"
          echo "============================================"
          echo "Target date: ${TARGET_DATE:-yesterday}"
          echo "Days to generate: $DAYS"
          echo "Database: $DB_NAME"
          echo "============================================"
          
          chmod +x scripts/sales_generator.py
          
          # Generate for single date or multiple days
          if [ -z "$TARGET_DATE" ]; then
            python scripts/sales_generator.py --db "$DB_NAME"
          else
            python scripts/sales_generator.py --date "$TARGET_DATE" --db "$DB_NAME"
          fi
          
          # Generate multiple days if requested
          if [ "$DAYS" -gt 1 ]; then
            if [ -z "$TARGET_DATE" ]; then
              START_DATE=$(date -d 'yesterday' +%Y-%m-%d)
            else
              START_DATE="$TARGET_DATE"
            fi
            
            for i in $(seq 2 $DAYS); do
              CURRENT_DATE=$(date -d "$START_DATE - $((i-1)) days" +%Y-%m-%d)
              echo "Processing: $CURRENT_DATE"
              python scripts/sales_generator.py --date "$CURRENT_DATE" --db "$DB_NAME"
            done
          fi
          
          # Get database info
          if [ -f "$DB_NAME" ]; then
            DB_SIZE=$(du -h "$DB_NAME" | cut -f1)
            echo "db_size=$DB_SIZE" >> $GITHUB_OUTPUT
            
            # Count records - FIXED INDENTATION
            python3 << 'EOF'
import sqlite3
import os
db_name = os.environ.get('DB_NAME', 'sales_monitoring.db')
try:
    conn = sqlite3.connect(db_name)
    cursor = conn.cursor()
    cursor.execute('SELECT COUNT(*) FROM sales_data')
    count = cursor.fetchone()[0]
    conn.close()
    # Append to GITHUB_OUTPUT
    with open(os.environ['GITHUB_OUTPUT'], 'a') as f:
        f.write(f'record_count={count}\n')
except Exception as e:
    with open(os.environ['GITHUB_OUTPUT'], 'a') as f:
        f.write('record_count=0\n')
    print(f"Error: {e}")
EOF
          else
            echo "db_size=0" >> $GITHUB_OUTPUT
            echo "record_count=0" >> $GITHUB_OUTPUT
            echo "❌ Database file not created"
            exit 1
          fi
      
      - name: Upload database artifact
        uses: actions/upload-artifact@v4
        with:
          name: sales-data-${{ github.run_id }}
          path: ${{ github.event.inputs.database_name }}
          retention-days: 30
      
      - name: Generate data summary
        if: always()
        run: |
          DB_NAME="${{ github.event.inputs.database_name }}"
          RECORD_COUNT="${{ steps.generate.outputs.record_count }}"
          DB_SIZE="${{ steps.generate.outputs.db_size }}"
          
          echo "============================================"
          echo "DATA GENERATION SUMMARY"
          echo "============================================"
          echo "Run ID: ${{ github.run_id }}"
          echo "Run number: ${{ github.run_number }}"
          echo "Triggered by: ${{ github.actor }}"
          echo "Date: $(date)"
          echo ""
          echo "Database: $DB_NAME"
          echo "Size: ${DB_SIZE:-0}"
          echo "Records: ${RECORD_COUNT:-0}"
          echo ""
          
          if [ -f "$DB_NAME" ] && [ "${RECORD_COUNT:-0}" -gt 0 ]; then
            echo "📊 SAMPLE DATA (last 10 records):"
            echo "----------------------------------------"
            python3 << EOF
import sqlite3
import pandas as pd
conn = sqlite3.connect('$DB_NAME')
df = pd.read_sql_query('''
    SELECT date, store_id, product_id, units_sold, revenue, 
           promotion_flag, stockout_flag
    FROM sales_data 
    ORDER BY date DESC, store_id, product_id 
    LIMIT 10
''', conn)
print(df.to_string(index=False))
conn.close()
EOF
            echo ""
            echo "----------------------------------------"
            echo ""
            echo "📊 SUMMARY BY STORE:"
            python3 << EOF
import sqlite3
conn = sqlite3.connect('$DB_NAME')
cursor = conn.cursor()
cursor.execute('''
    SELECT store_id, 
           COUNT(*) as transactions,
           SUM(units_sold) as total_units,
           SUM(revenue) as total_revenue,
           SUM(CASE WHEN promotion_flag=1 THEN 1 ELSE 0 END) as promos
    FROM sales_data
    GROUP BY store_id
    ORDER BY store_id
''')
results = cursor.fetchall()
print(f"{'Store':<6} {'Transactions':<14} {'Total Units':<12} {'Total Revenue':<15} {'Promos':<8}")
print("-" * 65)
for row in results:
    print(f"{row[0]:<6} {row[1]:<14} {row[2]:<12} {row[3]:<15.2f} {row[4]:<8}")
conn.close()
EOF
            echo ""
            echo "✅ Artifact available: sales-data-${{ github.run_id }}"
          else
            echo "⚠️ Database has no records"
          fi
          echo "============================================"
      
      - name: Completion status
        if: success()
        run: |
          echo "✅ Sales data generation completed successfully!"
          echo "📥 Download artifact: sales-data-${{ github.run_id }}"
      
      - name: Failure notification
        if: failure()
        run: |
          echo "❌ Sales data generation failed!"
          echo "Check logs above for details."
          exit 1
